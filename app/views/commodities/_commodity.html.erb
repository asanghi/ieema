<tr>
  <td><%= commodity_counter + 1 %></td>
  <td><%= link_to(commodity.name,commodity_path(commodity)) %></td>
  <td><code><%= commodity.code %></code></td>
  <% commodity.commodity_prices.for_year(@for_year).each do |cp| %>
    <td><%= cp.price %></td>
  <% end %>
  <td></td>
<tr>
